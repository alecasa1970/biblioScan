# Chave da Google Cloud Vision API (habilite "Cloud Vision API" no seu
# projeto do Google Cloud Console e gere uma API key em "Credenciais").
# Sem essa variável, o app roda em modo simulado (mock), permitindo digitar
# a marca manualmente para testar o restante do fluxo.
GOOGLE_VISION_API_KEY=

PORT=3000
